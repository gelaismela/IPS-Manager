# Production environment for Nginx reverse proxy
# Use same-origin path and let Nginx route /api -> Spring Boot :8080
REACT_APP_API_BASE=/api
# For backward compatibility, also set the legacy var (not required if above is set)
# REACT_APP_API_BASE_URL=/api